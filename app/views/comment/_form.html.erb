<%= form_with model: [ @post, @post.comment.build ] do |form| %>
  <p>
    <%= form.label :body %><br>
    <%= form.text_area :body %>
  </p>
  <p>
    <%= form.submit 'Add a comment'%>
  </p>
<% end %>